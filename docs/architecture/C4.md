# Architecture Map — Drake_Models

This document defines the canonical Mermaid C4 architecture map for `Drake_Models`, providing verified context, container boundaries, capability-to-component traceability, and architecture change history.

## Context View (C4Context)

```mermaid
C4Context
    title System Context diagram for Drake_Models

    Person(biomech_researcher, "Biomechanics Researcher", "Conducts movement analysis, strength training simulations, and trajectory optimization.")
    System(drake_models, "Drake_Models", "Generates parametric SDFormat (SDF 1.8) multibody models of human bodies and Olympic barbells.")
    System_Ext(upstream_drift, "UpstreamDrift", "Consumes generated models for golf swing biomechanics and multi-body simulation.")
    System_Ext(drake_sim, "Drake / MultibodyPlant", "Multibody dynamics physics engine and optimization solver framework.")
    System_Ext(filesystem, "File System / CLI", "Receives exported SDFormat XML artifacts generated from command-line invocation.")

    Rel(biomech_researcher, drake_models, "Invokes CLI or Python API to generate models", "CLI / Python")
    Rel(drake_models, filesystem, "Writes SDF 1.8 model XML files", "File I/O")
    Rel(drake_models, drake_sim, "Loads multibody models into MultibodyPlant (optional)", "pydrake Parser")
    Rel(upstream_drift, drake_models, "Discovers model packs and resolves exercises", "biomech.model_pack entry point")
```

## Container View (C4Container)

```mermaid
C4Container
    title Container diagram for Drake_Models

    Person(user, "User / Downstream Caller", "Biomechanics researcher or downstream fleet caller.")

    Container_Boundary(drake_models_boundary, "Drake_Models System") {
        Container(cli_entry, "CLI & Console Script", "Python (__main__.py)", "CLI entry point and command-line argument parser.")
        Container(exercise_builders, "Exercise Builders", "Python (exercises/)", "Modular builders for squat, bench press, deadlift, snatch, clean & jerk, gait, and sit-to-stand.")
        Container(shared_body, "Anthropometric Body Model", "Python (shared/body/)", "Segmented human body assembly based on Winter 2009 anthropometrics.")
        Container(shared_barbell, "Barbell Model", "Python (shared/barbell/)", "IWF/IPF specification Olympic barbell three-link assembly.")
        Container(shared_utils, "Geometry & SDF Helpers", "Python (shared/utils/)", "Inertia math, bounding boxes, and SDFormat 1.8 XML serialization.")
        Container(dbc_contracts, "Design-by-Contract Guards", "Python (shared/contracts/)", "Precondition parameter validation and postcondition invariants.")
        Container(optimization, "Optimization & Kinematics", "Python (optimization/)", "Trajectory costs, exercise objectives, and inverse-kinematics helpers.")
        Container(model_pack, "Model Pack Discovery", "Python (entry points)", "Publishes model_pack.yaml and biomech.model_pack registration.")
    }

    System_Ext(drake_engine, "Drake Simulation Engine", "Physics and trajectory optimization runtime.")

    Rel(user, cli_entry, "Executes", "python -m drake_models")
    Rel(user, exercise_builders, "Invokes model factory functions", "Python API")
    Rel(cli_entry, exercise_builders, "Delegates build commands", "Python")
    Rel(exercise_builders, dbc_contracts, "Validates parameters & outputs", "Python")
    Rel(exercise_builders, shared_body, "Constructs human kinematics & inertia", "Python")
    Rel(exercise_builders, shared_barbell, "Attaches barbell assembly", "Python")
    Rel(shared_body, shared_utils, "Formats SDF XML & calculates inertia", "Python")
    Rel(shared_barbell, shared_utils, "Calculates inertia tensors", "Python")
    Rel(exercise_builders, optimization, "Supplies exercise bounds & objectives", "Python")
    Rel(optimization, drake_engine, "Formulates trajectory optimization problems", "pydrake API")
```

## Feature Map

| Capability | Component | Interface | Evidence |
| --- | --- | --- | --- |
| Model Pack Discovery | `drake_models.shared` | `biomech.model_pack` entry point / `model_pack.yaml` | `tests/test_model_pack.py` |
| Anthropometric Body Modeling | `drake_models.shared.body` | `build_body_model()` / `BodyModelConfig` | `tests/unit/test_body_model.py` |
| Olympic Barbell Construction | `drake_models.shared.barbell` | `build_barbell_model()` / `BarbellConfig` | `tests/unit/test_barbell_model.py` |
| Back Squat Model Generation | `drake_models.exercises.squat` | `build_squat_model()` | `tests/unit/test_squat.py` |
| Bench Press Model Generation | `drake_models.exercises.bench_press` | `build_bench_press_model()` | `tests/unit/test_bench_press.py` |
| Deadlift Model Generation | `drake_models.exercises.deadlift` | `build_deadlift_model()` | `tests/unit/test_deadlift.py` |
| Snatch Model Generation | `drake_models.exercises.snatch` | `build_snatch_model()` | `tests/unit/test_snatch.py` |
| Clean & Jerk Generation | `drake_models.exercises.clean_and_jerk` | `build_clean_and_jerk_model()` | `tests/unit/test_clean_and_jerk.py` |
| Design-by-Contract Validation | `drake_models.shared.contracts` | `require()`, `ensure()` guards | `tests/test_drake_biomechanics_validation.py` |
| Drake Trajectory Optimization | `drake_models.optimization` | `DrakeTrajectorySolver`, exercise objectives | `tests/unit/test_optimization.py` |
| SDFormat 1.8 XML Generation | `drake_models.shared.utils` | `sdf_helpers.py`, `geometry.py` | `tests/unit/test_sdf_helpers.py` |

## Architecture Change Log

| Date | PR / Issue | Description |
| --- | --- | --- |
| 2026-09-10 | #1598 | Adopt maintainable Mermaid C4 architecture-map contract and automated CI verification |
